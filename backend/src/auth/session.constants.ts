// Name of the httpOnly cookie carrying the opaque session token.
export const SESSION_COOKIE = 'sid';

// Absolute session lifetime (not sliding): 7 days.
export const SESSION_TTL_MS = 7 * 24 * 60 * 60 * 1000;
