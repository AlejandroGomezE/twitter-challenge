# Installed and built inside the image
node_modules
dist
*.tsbuildinfo
# Regenerated by `prisma generate` in the build stage
src/generated

# Local secrets and databases (the container's DB lives on the /data volume)
.env*
prisma/*.db*

# Test artifacts and logs
coverage
.nyc_output
test-results
logs
*.log

# Editor / VCS / OS noise
.git
.idea
.vscode
.DS_Store
