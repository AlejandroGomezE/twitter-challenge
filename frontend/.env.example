# Base URL of the backend API (no trailing slash). Optional: defaults to
# http://localhost:3000 when unset. Vite bakes it into the bundle at build time,
# so a change needs a restart of the dev server (or a rebuild).
VITE_API_URL=http://localhost:3000
